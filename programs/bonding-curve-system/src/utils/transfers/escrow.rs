use anchor_lang::prelude::*;

/// Transfer NFT to escrow account
pub fn transfer_nft_to_escrow() -> Result<()> {
    // Implementation will be added
    Ok(())
}

/// Return NFT from escrow
pub fn return_nft_from_escrow() -> Result<()> {
    // Implementation will be added 
    Ok(())
}
